# Solides par défaut sont dans Scene.cpp (buildSceneCore).
# Ajoute des lignes "box cx cy halfW halfH tag" pour du niveau en plus (mods).
# tag: grass | stone | platform | redrock
#
# Le caillou rouge de test est déjà défini dans Scene.cpp — ne pas dupliquer ici
# (sinon double collision invisible / décalage client selon cwd).
